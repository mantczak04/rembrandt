Because of this text:

<text>
Rembrandt
Camera pose configurator

Tune camera-sphere coverage on your object, then save a YAML config for rembrandt-render.

API: connected (ok)
</text>

written on frontend page, the crucial content is not fully shown and user needs to scroll a bit to see everything correctly. Fix this. I think we just don't need big "Camera pose configurator" there.